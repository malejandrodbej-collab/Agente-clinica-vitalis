# Copia este archivo como ".env" y coloca tu clave real. Nunca subas ".env" a GitHub.
# Los embeddings (ingest.py y agent.py) corren localmente con HuggingFace, sin costo.
# El LLM de generación de respuestas usa Groq, que sí requiere clave (tiene capa gratuita).
GROQ_API_KEY=tu_clave_aqui
